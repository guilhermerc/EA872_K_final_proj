#ifndef ACTION_TYPE_HPP
#define ACTION_TYPE_HPP

namespace Controller
{

    enum ActionType
    {
        MOVE_UP,
        MOVE_RIGHT,
        MOVE_DOWN,
        MOVE_LEFT,
        SPAWN_BOMB
    };

}

#endif
